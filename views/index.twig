{% extends "layouts/default.twig" %}

{% block content %}
	{% if posts %}
		{% if title %}
			{% include 'partials/page-title.twig' %}
		{% endif %}

		{% for post in posts %}
			{% include 'partials/content.twig' %}
		{% endfor %}

		{% set pagination_opts = posts.pagination({show_all: false, mid_size: 3, end_size: 2}) %}
		{% include 'partials/pagination.twig' with { pagination: pagination_opts } %}
	{% else %}
		{% include 'partials/content-none.twig' %}
	{% endif %}
{% endblock %}
