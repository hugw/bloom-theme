{% extends "layouts/default.twig" %}

{% block body %}
	{% include "partials/#{type}.twig" %}

  {% if post.comment_status == "open" or post.comments %}
    {{ fn('comments_template') }}
  {% endif %}
{% endblock %}
